# JavaDOS installation process #

1. Install Xubuntu
2. Execute installation package / script
3. done!

fix sever01 static ip systemd
upload javados to proxmox javados vm via vsftpd

try javados 0.5.3 beta installer (on desktop) in proxmox xubuntu vm

add font installer to jdos 0.5.4 installer!
and try adb shell etc...